name = "raden-rahmat-malang"
main = "receive-email.js"
compatibility_date = "2024-01-01"

# ── Daftarkan binding untuk Email Routing ────────────────────
[vars]
# Variabel di sini bersifat PUBLIC (jangan taruh secret di sini!)
# Secret (TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID) disimpan via:
#   wrangler secret put TELEGRAM_BOT_TOKEN
#   wrangler secret put TELEGRAM_CHAT_ID

# ── Contoh penggunaan Workers Routes (opsional) ──────────────
# [[routes]]
# pattern = "mail.uniramalang.eu.cc/*"
# zone_name = "uniramalang.eu.cc"
